export const MEDIA_ACTIONS_KEY = '@ptown/media-actions/v1';
export type MediaActionPriority = 'High' | 'Normal' | 'Low';
export type MediaAction = { id: string; draftId: string; draftTitle: string; action: string; owner: string; priority: MediaActionPriority; dueDate: string; completed: boolean; updatedAt: string };
export function readMediaActions(value: string | null): MediaAction[] { if (!value) return []; const parsed: unknown = JSON.parse(value); if (!Array.isArray(parsed)) throw new Error('Invalid actions'); return parsed.filter((item): item is MediaAction => Boolean(item && typeof item === 'object' && typeof (item as MediaAction).id === 'string' && typeof (item as MediaAction).draftId === 'string' && typeof (item as MediaAction).draftTitle === 'string' && typeof (item as MediaAction).action === 'string' && typeof (item as MediaAction).owner === 'string' && ['High', 'Normal', 'Low'].includes((item as MediaAction).priority) && typeof (item as MediaAction).dueDate === 'string' && typeof (item as MediaAction).completed === 'boolean' && typeof (item as MediaAction).updatedAt === 'string')); }
