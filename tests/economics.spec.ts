import { expect, test } from '@playwright/test';
test('economics calculates profitability and creates an unsent offer',async({page})=>{await page.goto('/operations/economics');await page.getByLabel('Proposed event date').fill('2027-08-07');await page.getByLabel('Artist guarantee').fill('10000');await expect(page.getByText('Projected profit')).toBeVisible();await page.getByText('Save economics worksheet').click();await expect(page.getByText('Economics worksheet saved.')).toBeVisible();await page.getByText('Save unsent offer draft').click();await expect(page.getByText('Nothing has been sent.')).toBeVisible();await expect(page.getByText('Draft',{exact:true}).last()).toBeVisible();});
