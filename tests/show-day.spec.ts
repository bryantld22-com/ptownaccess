import { expect, test } from '@playwright/test';
test('show-day advance and settlement save operational records',async({page})=>{await page.goto('/operations/show-day');await page.getByLabel('Event date').fill('2027-08-07');await page.getByLabel('Production lead').fill('PTown Production Director');await page.getByText('Soundcheck complete').click();await page.getByText('Save show-day advance').click();await expect(page.getByText('Show-day advance saved on this device.')).toBeVisible();await page.goto('/operations/settlement');await page.getByLabel('Event date').fill('2027-08-07');await page.getByLabel('Paid attendance').fill('220');await page.getByLabel('Ticket gross').fill('12100');await page.getByLabel('Artist payment').fill('5000');await page.getByText('Save settlement').click();await expect(page.getByText('Event settlement saved on this device.')).toBeVisible();});
