import{expect,test}from'@playwright/test';test('production readiness is honest about local preview state',async({page})=>{await page.goto('/operations/production-readiness');await expect(page.getByText('PREVIEW MODE')).toBeVisible();await expect(page.getByText('Device-only preview storage')).toBeVisible();await expect(page.getByText('Authenticated staff accounts')).toBeVisible();await expect(page.getByText('No operational record is represented as cloud-synced')).toBeVisible();});
