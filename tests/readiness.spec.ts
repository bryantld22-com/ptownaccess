import{expect,test}from'@playwright/test';test('readiness dashboard tracks due dates',async({page})=>{await page.goto('/operations/readiness');await page.getByLabel('Task or document').fill('Certificate of insurance renewal');await page.getByText('Document expiration',{exact:true}).click();await page.getByLabel('Due or expiration date').fill('2027-07-15');await page.getByText('Save due date').click();await expect(page.getByText('Management due date saved.')).toBeVisible();await expect(page.getByText('Certificate of insurance renewal')).toBeVisible();});
